# 未確定の論点

## 解決済み（スコープ外として決定）
以下の項目はスコープ外として決定されたため、検討不要：
- ~~Google OAuthの同意画面と必要スコープの範囲~~ → ADR-0014: OAuth認証は実装しない
- ~~動画配信の方式（YouTube/自前/埋め込み）と計測APIの制約~~ → ADR-0015: 動画プレイヤー連携は実装しない
- ~~再生速度変更やシークの扱い（無効化/補正/再視聴要求）~~ → ADR-0015
- ~~視聴完了判定の閾値（100%必須か、許容範囲を設けるか）~~ → ADR-0015
- ~~動画視聴のみのIN/OUTをどのUI/体験で入力させるか~~ → ADR-0015: 手動IN/OUTのみ
- ~~受講者の講座絞り込みをClassroom APIでどこまで自動化できるか~~ → ADR-0006改訂: 手入力のみ
- ~~Classroom遷移後のタブ閉鎖検知はどこまで可能か（ブラウザ制約）~~ → ADR-0021: スコープ外（ブラウザセキュリティ制約）

## 解決済み（認証方式）
- ~~本番運用時の認証方式（Basic認証、独自ID/PW、招待リンク等）~~ → ADR-0016: Firebase Authentication + Googleソーシャルログインを採用

## 解決済み（セッション管理）
- ~~OUTが欠損したセッションの確定条件（時間経過/手動）~~ → ADR-0020: 48時間経過で自動クローズ
- ~~別タブ遷移時にアプリのheartbeatを継続する方式（同一タブ/別タブ）~~ → ADR-0022: setInterval継続（現行維持）
- ~~同時に複数講座のセッションが重複した場合の扱い~~ → ADR-0023: 同時1セッションのみ許可

## 解決済み（通知手段）
- ~~通知手段（SendGrid/Gmail API/SMTP）の選定~~ → Gmail API採用（実装済み）

## 残る論点
（現時点で未確定の論点はありません）
