# サーバーポート
PORT=8080

# 認証モード: "dev" または "firebase"
AUTH_MODE=dev

# Firebase設定（AUTH_MODE=firebaseの場合に必要）
FIREBASE_PROJECT_ID=classroom-checkin-279

# GCP認証（ローカル開発用）
# Cloud Runでは自動的にサービスアカウントが使用される
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# Firestoreプロジェクト（自動検出されない場合に指定）
# FIRESTORE_PROJECT_ID=classroom-checkin-279

# CORS設定（本番環境では必須）
# CORS_ORIGIN=https://web-102013220292.asia-northeast1.run.app
